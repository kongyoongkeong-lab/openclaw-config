# OpenClaw Configuration

该仓库存放 OpenClaw Gateway 配置文件。

## 使用方式

```bash
# 从工作站链接配置
ln -sf ~/openclaw-stack/openclaw-config/config.yaml ~/.openclaw/config.yaml
ln -sf ~/openclaw-stack/openclaw-config/gateway.yaml ~/.openclaw/gateway.yaml
```
