# OpenClaw Gateway Configuration
# Managed via openclaw-config repo

# Port
port: 8080

# Security
token:
  enabled: true

# Logging
logging:
  level: info
  format: json

# Channels
channels:
  - type: webchat
